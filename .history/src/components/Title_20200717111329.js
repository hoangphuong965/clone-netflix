import React from 'react'

function Title({title, subtitle}) {
     return (
          <div>
               <h1>{title}</h1>
               <sub></sub>
          </div>
     )
}

export default Title
